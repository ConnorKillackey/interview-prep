import java.util.*;


class GraphProblems {

    // find the center of a graph
    // Given a bipartite graph, separate the vertices into two sets.  
    // check if route between two nodes in graph

}
